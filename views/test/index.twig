{% extends 'layout.twig' %}

{% block body %}
  <h1>{{title}}</h1>
  <table border="1">
    <thead>
      <tr>
        <th>id</th>
        <th>c1</th>
        <th>c2</th>
      </tr>
    </thead>
    <tbody>
      {% for row in rows %}
      <tr>
        <td>{{row['id']}}</td>
        <td>{{row['c1']}}</td>
        <td>{{row['c2']}}</td>
        <td><a href="/test/edit?id={{row['id']}}">編輯</a>
      </tr>
      {% endfor %}
    </tbody>
  </table>
{% endblock %}
